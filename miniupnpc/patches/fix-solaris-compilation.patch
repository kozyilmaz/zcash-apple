diff --git a/Makefile b/Makefile
index 00d1937..337201a 100644
--- a/Makefile
+++ b/Makefile
@@ -43,10 +43,12 @@ CFLAGS += -D_NETBSD_SOURCE
 endif
 ifneq ($(OS), FreeBSD)
 ifneq ($(OS), Darwin)
+ifneq ($(OS), SunOS)
 #CFLAGS += -D_POSIX_C_SOURCE=200112L
 CFLAGS += -D_XOPEN_SOURCE=600
 endif
 endif
+endif
 #CFLAGS += -ansi
 # -DNO_GETADDRINFO
 INSTALL = install
